{
  "version": "0.2.381",
  "date": "2026-09-12",
  "groups": [
    {
      "title": "修复",
      "items": [
        "**「并入」被误判为失败，把整条队列卡住**：模型正在回答时插一句话（并入），界面有时会弹出「并入结果无法确认，已暂停后续队列」，还给出「取回为新消息 / 保留不发」两个选择。查下来这是**误判**——并入其实**已经成功写进了会话记录**，只是它落盘要等下一次工具执行完（实测等待从 3 秒到 148 秒不等），而界面的确认环节只等固定 20 秒，超时就判失败，且判定后不会自动回头。现在改成：**只要这一轮还在跑就一直等下去**，只有这一轮彻底结束后内容仍然找不到，才判失败。**真的失败时照样会拦住队列，不会漏**。"
      ]
    }
  ]
}
