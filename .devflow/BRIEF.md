# BRIEF — 第 221 轮批量修复/功能(dev-flow 标准任务)

起点 HEAD: d94d5d1 · 已发版 v0.2.220 · 分支 master

## 范围(7 项,用户明确"全做")

1. **[bug] 文件树 ⋮ 菜单真机无反应**:WKWebView 里点 ⋮ 不弹菜单(dev Chromium 看着正常)。
2. **[feature] 思考/工具折叠增强**:现状已默认折叠+可展开。用户要:①思考块折叠时显示一句话摘要(非通用"思考过程");②流式进行时折叠条显示动态状态("正在读取 X"/"正在思考…")。仿 Claude Desktop。
3. **[bug+feature] 停靠预览不实时刷新**:停靠板块冻结在点击瞬间的代码快照,不随流式更新。要:停靠随流式实时刷新+实时渲染;点停靠后会话内联渲染折叠为代码块(仍保留预览/代码/停靠按钮)。**用户决定:本轮只修冻结 bug + 内联折叠;p5.js/three.js 实时渲染以后再说(现状 html/svg/mermaid 可预览,p5/three 需包进 html)**。
4. **[已定:不做]** 调研确认:GUI 内派发的子代理**不会**进会话列表(子目录物理隔离 + isSidechain 兜底双保险),用户直觉对。用户看到"一会话多条"= Desktop 侧 compact/resume 拆成多个顶层 jsonl、collapseCompactChains 折叠未成功(跨文件 boundary uuid 没连上),与子代理无关。**#4 移出本轮范围**。(遗留可选:折叠健壮性——非本轮,用户未要求。)
5. **[small] worktree 列表不实时刷新**:AI 建的 worktree 其实已能在"worktree"按钮里看到(git worktree list 位置无关),唯一缺口=弹层只在打开那刻拉一次,不实时刷新。补刷新按钮/轮询。
6. **[small] 外部改 settings.json 不同步显示**:功能层已自动生效(compatKey 含 mtime),但设置面板显示层只在打开时拉一次,不实时。补 settings.json 文件监听→推送→显示层自动刷新。
7. **[feature] 图片点击放大预览**:输入框里的图片、已发送消息里的图片,点击都应能放大预览(lightbox)。**文件不需要预览**。**[已定]交互=单击放大,去掉图片双击;"用默认App打开"收进 lightbox 内按钮;文件卡片保持双击打开不变**。

## 用户确认的决策
- p5/three 本轮不做,只修停靠冻结 + 内联折叠。
- 思考折叠要摘要 + 流式动态状态两者都做。
- 严格走 dev-flow 全流程。
- fable 额度已用尽,实现代理只能用 opus。

## 敏感面声明
- 无新增外部网络/密钥/用户数据外发。
- #1 portal、#2/#3 前端渲染、#5 前端刷新、#7 前端 lightbox 纯客户端。
- #6 需后端加 settings.json 文件监听(fs.watch)+ WS 推送(读文件 mtime,不读内容明文外发,只推"配置变了,请重拉"信号或新配置对象——方案代理定)。
- #4 收紧过滤=只读枚举逻辑,无写。

## 验收基调
- 前端可见改动:dev 浏览器 + 真机(#1 只有真机能真验)双验。
- #1 真机验证是硬门(dev Chromium 复现不了)。
